services:
  asistente-bot:
    build:
      context: .
    ports:
      - "3101:80"
    environment:
      NODE_ENV: development
      HOST: 0.0.0.0
      HOSTNAME: 0.0.0.0
      PORT: "80"
    restart: unless-stopped
