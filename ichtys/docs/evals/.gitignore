# Resultados manuales del smoke test
# Estos archivos no deben commitearse: pueden contener respuestas de Ichtys,
# excerpts de documentos o datos de sesión.
mock-metabolic-smoke-test-results-*.csv

# Resultados automáticos del eval runner (Phase 10B)
# Pueden contener snippets de respuestas del LLM — nunca commitear.
results/

# Archivos de resultados sueltos (cualquier patrón)
*.eval-results.json
*.eval-results.csv

# PDFs generados desde los Markdown mock
# No commitear salvo decisión explícita del Comandante.
mock-metabolic-documents/*.pdf
